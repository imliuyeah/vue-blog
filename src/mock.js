const mock = require('mockjs')
const Random = Mock.Random
const domain = 'http://mockjs.com/api'
const code = 200

const postData = req => {
  console.log(req)
  let posts = []
  for(let i = 0; i < 10; i++){
    let post = {
      
    }
  }
}